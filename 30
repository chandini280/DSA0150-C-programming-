#include<iostream>
using namespace std;
int main()
{
	int n,r,sum=0,temp;
	cout<<"enter a value n";
	cin>>n;
	temp=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(sum==temp)
	{
		cout<<"it is an armstrong";
	}
	else
	cout<<"it is not an armstrong";
}
