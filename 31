#include<iostream>
using namespace std;
int main()
{
	int n,r,sum=0;
	cout<<"enter a number";
	cin>>n;
	while(n>0)
	{
		n=n%10;
		sum=sum+r;
		n=n/10;
	}
	if(n%sum==0)
	{
		cout<<"it is a harshad number";
	}
	else
	cout<<"it is not a harshad number";
	return 0;
}
